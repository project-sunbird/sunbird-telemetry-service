---
description: Run tests using Mocha/Chai/Sinon for the telemetry service
---

Use the /test skill to run the appropriate tests. If a target is specified, run only those tests. Otherwise, determine what to test from recent changes.

All commands must be run from the `src/` directory.

Test target: $ARGUMENTS
